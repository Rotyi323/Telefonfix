import { PriceSpacePipe } from './price-space.pipe';

describe('PriceSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
